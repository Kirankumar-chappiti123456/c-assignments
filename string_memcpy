#include <stdio.h>
#include <string.h>

int main () {
   char src[50] = "kiran";
   char des[50]="kumar";
   printf("Before memcpy des = %s\n", des);
   memcpy(des, src,sizeof(src));
   printf("After memcpy dest = %s\n", des);
   return 0;
}
output:
Before memcpy des = kumar
After memcpy dest = kiran
